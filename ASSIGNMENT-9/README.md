Assignment 9 (Switch Case)
Write a Java program that:
Takes an option number from the user.
Uses a switch-case statement to select a language.
If the user selects:
1 → Print "Selected language is Bengali"
2 → Print "Selected language is Hindi"
3 → Print "Selected language is Urdu"
Any other option → Print "Selected language is English"
